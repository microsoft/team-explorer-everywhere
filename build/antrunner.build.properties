# These properties are merged with all the properties that were read by 
# earlier build stages when when antrunner.build.xml is executed.
# 
# Put settings here that affect how Eclipse's Ant Runner builds
# the modules.  Don't put settings here to change the way the
# build stages run, use the machine configuration files for that instead.

javacFailOnError=true
javacVerbose=false
javacSource=1.6
javacTarget=1.6
build.compiler=modern
