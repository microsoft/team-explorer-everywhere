Taglets is a collection of add-ons for Javadoc processing.  It provides some Doclets
that look nicer than the standard ones (allow for lots of custom CSS, etc.).  They
also allow for easy custom block/inline tags.

The build modules that need to build javadoc should use these.