# This is a properties file for PDE build to use.  See the PDE build template
# that comes with Eclipse for other things you can set here.
# 
# Generally the macro "macro.build-feature" takes care of setting the 
# dynamic properties (build ID, paths, etc.).
 
javacFailOnError=true
javacVerbose=false
javacSource=1.5
javacTarget=1.5
build.compiler=modern

