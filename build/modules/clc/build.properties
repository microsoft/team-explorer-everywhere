id.feature.clc=com.microsoft.tfs.client.clc.feature

# Where the final archive package is placed in the binaries directory
dir.binaries.clc=${dir.binaries}/clc

# The names of the actual archive files (".zip") produced during build.
name.filename-prefix.nlz=TEE-CLC-NLZ-${string.version.1-2-3}
name.filename-prefix.nl1=TEE-CLC-NL1-${string.version.1-2-3}
name.filename-prefix.clc=TEE-CLC-${string.version.1-2-3}

# The file prefix used for all items inside the archives.
name.archive-prefix.clc=TEE-CLC-${string.version.1-2-3}

