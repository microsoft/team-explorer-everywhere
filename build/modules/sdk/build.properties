# The name of the output archive
name.filename-prefix.sdk-singlejar=TFS-SDK-${string.version.1-2-3}

# The name of the jar file for single jar output
name.filename-prefix.jar-singlejar=com.microsoft.tfs.sdk-${string.version.1-2-3}

# The file prefix used for all items inside the archives.
name.archive-prefix.sdk=TFS-SDK-${string.version.1-2-3}

