// Copyright (c) Microsoft. All rights reserved.
// Licensed under the MIT license. See License.txt in the repository root.

 /*
 * This file was automatically generated by com.microsoft.tfs.core.ws.generator.Generator
 * from the /complexType.vm template.
 */
package ms.tfs.versioncontrol.clientservices._03;

import com.microsoft.tfs.core.ws.runtime.*;
import com.microsoft.tfs.core.ws.runtime.serialization.*;
import com.microsoft.tfs.core.ws.runtime.types.*;
import com.microsoft.tfs.core.ws.runtime.util.*;
import com.microsoft.tfs.core.ws.runtime.xml.*;

import ms.tfs.versioncontrol.clientservices._03._TeamProjectFolderPermission;

import java.lang.String;

import java.util.ArrayList;
import java.util.List;

import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;

/**
 * Automatically generated complex type class.
 */
public class _TeamProjectFolderPermission
    implements ElementSerializable, ElementDeserializable
{
    // Attributes
    protected String ident;

    // Elements
    protected String[] allowPermission;
    protected String[] denyPermission;

    public _TeamProjectFolderPermission()
    {
        super();
    }

    public _TeamProjectFolderPermission(
        final String ident,
        final String[] allowPermission,
        final String[] denyPermission)
    {
        // TODO : Call super() instead of setting all fields directly?
        setIdent(ident);
        setAllowPermission(allowPermission);
        setDenyPermission(denyPermission);
    }

    public String getIdent()
    {
        return this.ident;
    }

    public void setIdent(final String value)
    {
        this.ident = value;
    }

    public String[] getAllowPermission()
    {
        return this.allowPermission;
    }

    public void setAllowPermission(String[] value)
    {
        this.allowPermission = value;
    }

    public String[] getDenyPermission()
    {
        return this.denyPermission;
    }

    public void setDenyPermission(String[] value)
    {
        this.denyPermission = value;
    }

    public void writeAsElement(
        final XMLStreamWriter writer,
        final String name)
        throws XMLStreamException
    {
        writer.writeStartElement(name);

        // Attributes
        XMLStreamWriterHelper.writeAttribute(
            writer,
            "ident",
            this.ident);

        // Elements
        if (this.allowPermission != null)
        {
            /*
             * The element type is an array.
             */
            writer.writeStartElement("AllowPermission");

            for (int iterator0 = 0; iterator0 < this.allowPermission.length; iterator0++)
            {
                XMLStreamWriterHelper.writeElement(
                    writer,
                    "string",
                    this.allowPermission[iterator0]);
            }

            writer.writeEndElement();
        }

        if (this.denyPermission != null)
        {
            /*
             * The element type is an array.
             */
            writer.writeStartElement("DenyPermission");

            for (int iterator0 = 0; iterator0 < this.denyPermission.length; iterator0++)
            {
                XMLStreamWriterHelper.writeElement(
                    writer,
                    "string",
                    this.denyPermission[iterator0]);
            }

            writer.writeEndElement();
        }

        writer.writeEndElement();
    }

    public void readFromElement(final XMLStreamReader reader)
        throws XMLStreamException
    {
        String localName;

        // Attributes
        final int attributeCount = reader.getAttributeCount();
        String attributeValue;

        for (int i = 0; i < attributeCount; i++)
        {
            localName = reader.getAttributeLocalName(i);
            attributeValue = reader.getAttributeValue(i);

            if (localName.equalsIgnoreCase("ident"))
            {
                this.ident = attributeValue;
            }

            // Ignore unknown attributes.
        }

        // Elements
        int event;

        do
        {
            event = reader.next();

            if (event == XMLStreamConstants.START_ELEMENT)
            {
                localName = reader.getLocalName();

                if (localName.equalsIgnoreCase("AllowPermission"))
                {
                    /*
                     * The element type is an array.
                     */
                    int event0;
                    final List list0 = new ArrayList();

                    do
                    {
                        event0 = reader.nextTag();

                        if (event0 == XMLStreamConstants.START_ELEMENT)
                        {
                            list0.add(reader.getElementText());
                        }
                    }
                    while (event0 != XMLStreamConstants.END_ELEMENT);

                    this.allowPermission = (String[]) list0.toArray(new String[list0.size()]);
                }
                else if (localName.equalsIgnoreCase("DenyPermission"))
                {
                    /*
                     * The element type is an array.
                     */
                    int event0;
                    final List list0 = new ArrayList();

                    do
                    {
                        event0 = reader.nextTag();

                        if (event0 == XMLStreamConstants.START_ELEMENT)
                        {
                            list0.add(reader.getElementText());
                        }
                    }
                    while (event0 != XMLStreamConstants.END_ELEMENT);

                    this.denyPermission = (String[]) list0.toArray(new String[list0.size()]);
                }
                else
                {
                    // Read the unknown child element until its end
                    XMLStreamReaderHelper.readUntilElementEnd(reader);
                }
            }
        }
        while (event != XMLStreamConstants.END_ELEMENT);
    }
}
