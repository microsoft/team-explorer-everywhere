// Copyright (c) Microsoft. All rights reserved.
// Licensed under the MIT license. See License.txt in the repository root.

 /*
 * This file was automatically generated by com.microsoft.tfs.core.ws.generator.Generator
 * from the /complexType.vm template.
 */
package ms.tfs.versioncontrol.clientservices._03;

import com.microsoft.tfs.core.ws.runtime.*;
import com.microsoft.tfs.core.ws.runtime.serialization.*;
import com.microsoft.tfs.core.ws.runtime.types.*;
import com.microsoft.tfs.core.ws.runtime.util.*;
import com.microsoft.tfs.core.ws.runtime.xml.*;

import ms.tfs.versioncontrol.clientservices._03._ChangeType;
import ms.tfs.versioncontrol.clientservices._03._Failure;
import ms.tfs.versioncontrol.clientservices._03._RepositorySoap_UpdateItemSecurityResponse;
import ms.tfs.versioncontrol.clientservices._03._RequestType;
import ms.tfs.versioncontrol.clientservices._03._SecurityChange;
import ms.tfs.versioncontrol.clientservices._03._SeverityType;
import ms.tfs.versioncontrol.clientservices._03._Warning;
import ms.tfs.versioncontrol.clientservices._03._WarningType;

import java.lang.String;

import java.util.ArrayList;
import java.util.List;

import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;

/**
 * Automatically generated complex type class.
 */
public class _RepositorySoap_UpdateItemSecurityResponse
    implements ElementDeserializable
{
    // No attributes    

    // Elements
    protected _SecurityChange[] updateItemSecurityResult;
    protected _Failure[] failures;

    public _RepositorySoap_UpdateItemSecurityResponse()
    {
        super();
    }

    public _RepositorySoap_UpdateItemSecurityResponse(
        final _SecurityChange[] updateItemSecurityResult,
        final _Failure[] failures)
    {
        // TODO : Call super() instead of setting all fields directly?
        setUpdateItemSecurityResult(updateItemSecurityResult);
        setFailures(failures);
    }

    public _SecurityChange[] getUpdateItemSecurityResult()
    {
        return this.updateItemSecurityResult;
    }

    public void setUpdateItemSecurityResult(_SecurityChange[] value)
    {
        this.updateItemSecurityResult = value;
    }

    public _Failure[] getFailures()
    {
        return this.failures;
    }

    public void setFailures(_Failure[] value)
    {
        this.failures = value;
    }

    public void readFromElement(final XMLStreamReader reader)
        throws XMLStreamException
    {
        String localName;

        // This object uses no attributes

        // Elements
        int event;

        do
        {
            event = reader.next();

            if (event == XMLStreamConstants.START_ELEMENT)
            {
                localName = reader.getLocalName();

                if (localName.equalsIgnoreCase("UpdateItemSecurityResult"))
                {
                    /*
                     * The element type is an array.
                     */
                    int event0;
                    final List list0 = new ArrayList();

                    do
                    {
                        event0 = reader.nextTag();

                        if (event0 == XMLStreamConstants.START_ELEMENT)
                        {
                            _SecurityChange complexObject0 = null;

                            /*
                             * The element is an abstract type, so prepare to deserialize all the
                             * known derived types.
                             */
                            final String typeAttribute = reader.getAttributeValue(
                                    Schemas.XSI,
                                    "type");

                            if (typeAttribute == null)
                            {
                                throw new XMLStreamException(
                                        "Element " + reader.getLocalName()
                                        + " is for a derived type but did not include a type attribute");
                            }

                            if (typeAttribute.equals("InheritanceChange"))
                            {
                                complexObject0 = new _InheritanceChange();
                            }
                            else if (typeAttribute.equals("PermissionChange"))
                            {
                                complexObject0 = new _PermissionChange();
                            }

                            complexObject0.readFromElement(reader);

                            list0.add(complexObject0);
                        }
                    }
                    while (event0 != XMLStreamConstants.END_ELEMENT);

                    this.updateItemSecurityResult = (_SecurityChange[]) list0.toArray(
                            new _SecurityChange[list0.size()]);
                }
                else if (localName.equalsIgnoreCase("failures"))
                {
                    /*
                     * The element type is an array.
                     */
                    int event0;
                    final List list0 = new ArrayList();

                    do
                    {
                        event0 = reader.nextTag();

                        if (event0 == XMLStreamConstants.START_ELEMENT)
                        {
                            _Failure complexObject0 = new _Failure();
                            complexObject0.readFromElement(reader);
                            list0.add(complexObject0);
                        }
                    }
                    while (event0 != XMLStreamConstants.END_ELEMENT);

                    this.failures = (_Failure[]) list0.toArray(new _Failure[list0.size()]);
                }
                else
                {
                    // Read the unknown child element until its end
                    XMLStreamReaderHelper.readUntilElementEnd(reader);
                }
            }
        }
        while (event != XMLStreamConstants.END_ELEMENT);
    }
}
