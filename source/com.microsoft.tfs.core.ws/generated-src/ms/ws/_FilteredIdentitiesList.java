// Copyright (c) Microsoft. All rights reserved.
// Licensed under the MIT license. See License.txt in the repository root.

 /*
 * This file was automatically generated by com.microsoft.tfs.core.ws.generator.Generator
 * from the /complexType.vm template.
 */
package ms.ws;

import com.microsoft.tfs.core.ws.runtime.*;
import com.microsoft.tfs.core.ws.runtime.serialization.*;
import com.microsoft.tfs.core.ws.runtime.types.*;
import com.microsoft.tfs.core.ws.runtime.util.*;
import com.microsoft.tfs.core.ws.runtime.xml.*;

import ms.ws._FilteredIdentitiesList;
import ms.ws._IdentityDescriptor;
import ms.ws._KeyValueOfStringString;
import ms.ws._PropertyValue;
import ms.ws._TeamFoundationIdentity;

import java.lang.Object;
import java.lang.String;

import java.util.ArrayList;
import java.util.List;

import javax.xml.stream.XMLStreamConstants;
import javax.xml.stream.XMLStreamException;
import javax.xml.stream.XMLStreamReader;
import javax.xml.stream.XMLStreamWriter;

/**
 * Automatically generated complex type class.
 */
public class _FilteredIdentitiesList
    implements ElementSerializable, ElementDeserializable
{
    // Attributes
    protected boolean hasMoreItems;
    protected int totalItems;
    protected int startingIndex;

    // Elements
    protected _TeamFoundationIdentity[] items;

    public _FilteredIdentitiesList()
    {
        super();
    }

    public _FilteredIdentitiesList(
        final boolean hasMoreItems,
        final int totalItems,
        final int startingIndex,
        final _TeamFoundationIdentity[] items)
    {
        // TODO : Call super() instead of setting all fields directly?
        setHasMoreItems(hasMoreItems);
        setTotalItems(totalItems);
        setStartingIndex(startingIndex);
        setItems(items);
    }

    public boolean isHasMoreItems()
    {
        return this.hasMoreItems;
    }

    public void setHasMoreItems(final boolean value)
    {
        this.hasMoreItems = value;
    }

    public int getTotalItems()
    {
        return this.totalItems;
    }

    public void setTotalItems(final int value)
    {
        this.totalItems = value;
    }

    public int getStartingIndex()
    {
        return this.startingIndex;
    }

    public void setStartingIndex(final int value)
    {
        this.startingIndex = value;
    }

    public _TeamFoundationIdentity[] getItems()
    {
        return this.items;
    }

    public void setItems(_TeamFoundationIdentity[] value)
    {
        this.items = value;
    }

    public void writeAsElement(
        final XMLStreamWriter writer,
        final String name)
        throws XMLStreamException
    {
        writer.writeStartElement(name);

        // Attributes
        XMLStreamWriterHelper.writeAttribute(
            writer,
            "HasMoreItems",
            this.hasMoreItems);
        XMLStreamWriterHelper.writeAttribute(
            writer,
            "TotalItems",
            this.totalItems);
        XMLStreamWriterHelper.writeAttribute(
            writer,
            "StartingIndex",
            this.startingIndex);

        // Elements
        if (this.items != null)
        {
            /*
             * The element type is an array.
             */
            writer.writeStartElement("Items");

            for (int iterator0 = 0; iterator0 < this.items.length; iterator0++)
            {
                this.items[iterator0].writeAsElement(
                    writer,
                    "TeamFoundationIdentity");
            }

            writer.writeEndElement();
        }

        writer.writeEndElement();
    }

    public void readFromElement(final XMLStreamReader reader)
        throws XMLStreamException
    {
        String localName;

        // Attributes
        final int attributeCount = reader.getAttributeCount();
        String attributeValue;

        for (int i = 0; i < attributeCount; i++)
        {
            localName = reader.getAttributeLocalName(i);
            attributeValue = reader.getAttributeValue(i);

            if (localName.equalsIgnoreCase("HasMoreItems"))
            {
                this.hasMoreItems = XMLConvert.toBoolean(attributeValue);
            }
            else if (localName.equalsIgnoreCase("TotalItems"))
            {
                this.totalItems = XMLConvert.toInt(attributeValue);
            }
            else if (localName.equalsIgnoreCase("StartingIndex"))
            {
                this.startingIndex = XMLConvert.toInt(attributeValue);
            }

            // Ignore unknown attributes.
        }

        // Elements
        int event;

        do
        {
            event = reader.next();

            if (event == XMLStreamConstants.START_ELEMENT)
            {
                localName = reader.getLocalName();

                if (localName.equalsIgnoreCase("Items"))
                {
                    /*
                     * The element type is an array.
                     */
                    int event0;
                    final List list0 = new ArrayList();

                    do
                    {
                        event0 = reader.nextTag();

                        if (event0 == XMLStreamConstants.START_ELEMENT)
                        {
                            _TeamFoundationIdentity complexObject0 = new _TeamFoundationIdentity();
                            complexObject0.readFromElement(reader);
                            list0.add(complexObject0);
                        }
                    }
                    while (event0 != XMLStreamConstants.END_ELEMENT);

                    this.items = (_TeamFoundationIdentity[]) list0.toArray(new _TeamFoundationIdentity[list0.size()]);
                }
                else
                {
                    // Read the unknown child element until its end
                    XMLStreamReaderHelper.readUntilElementEnd(reader);
                }
            }
        }
        while (event != XMLStreamConstants.END_ELEMENT);
    }
}
